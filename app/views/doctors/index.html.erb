<h1>Doctors</h1>
<hr />
<h2><%= @doctors.length <= 0 ? "No Doctor" : "" %></h2>
<ul>
  <% @doctors.each do |doctor| %>
    <li>
      <%= link_to doctor.full_name, doctor %>
    </li>
  <% end %>
</ul>